# 2026-07-31

## Nexa V0 规划文档补充

审阅了全部七份规划文档（PRD、User Flow、Architecture、Information Architecture、System Design、Tech Stack、FutureVision），识别了五个工程层面的 gap，并逐一补入对应文档：

### 补充内容概览

1. **Auth 方案** — PRD 新增 Authentication 章节，System Design 新增 Auth Service 和 Auth API，Tech-Stack 新增 JWT+bcrypt+python-jose，Architecture 新增 Auth Gateway 层和 Auth Flow
2. **流式响应 (SSE)** — Tech-Stack 新增 SSE (sse-starlette) 章节含事件类型定义，System Design 新增 Chat Stream API 端点，Information Architecture 新增 Streaming Progress Indicator UI，User Flow 新增实时进度状态，Architecture 新增 Streaming Controller 层和 Streaming Flow
3. **前端大数据渲染** — Tech-Stack 新增 3.6 节，推荐 ag-grid-community 做数据预览表（Ant Design 只做应用 chrome），PRD 新增数据量边界表
4. **Insight 灵活数据模型** — System Design 新增 Insight Content Model 章节，使用 JSON 字段存储非结构化分析结果（summary, key_findings, tables, charts, sql_queries, recommendations）
5. **Chat ↔ Notebook 桥接** — System Design 新增 Chat↔Notebook Bridge 章节含 Export API，Information Architecture 新增过渡 UI，User Flow 新增 Advanced: Open in Notebook 步骤

### 未修改
- FutureVision.md — 无需修改，纯愿景文档

### 执行计划
- 创建 Nexa V0 Execution Plan.md：6 个 Phase 串行执行，每个 Phase 含具体任务 checklist、验收标准、依赖关系说明

### UI 设计研究
- 确定布局方案：顶部 Tab 导航（不做侧边栏）
- 确定配色：黑白灰底 + 科技蓝 #2563EB 强调色，深色主题
- 确定 SSE 进度指示器：三步状态（done/running/pending）+ 颜色编码 + 脉冲动画
- 创建 Nexa V0 UI Spec.md：完整 UI 规范

### Phase 0-1 代码实现
- 前端：Vite+React+TS+AntD+ag-grid+ECharts+Zustand，页面（登录/注册/项目列表/项目工作台）
- 后端：FastAPI+SQLAlchemy+Alembic+Python 3.13，8 个数据模型，Auth+Project+Dataset API
- Docker：docker-compose.yml，各服务 Dockerfile，Nginx 配置
- TypeScript 编译通过，前端构建成功

### Phase 2-5 全部完成
- Phase 2：DuckDB 查询引擎 + ag-grid 数据预览（虚拟滚动，深色主题）
- Phase 3：AI Agent Controller + SSE 流式 Chat + 三步进度指示器（done/running/pending + 脉冲动画）
- Phase 4：Insight 保存/展示 + ECharts 图表渲染 + Chat "Save Insight" 按钮
- Phase 5：Notebook（Markdown/SQL/Python Cell）+ Chat→Notebook "Open in Notebook" 桥接
- 前端所有页面完整可用，TypeScript 构建通过（1.75 MB, 534 KB gzip）

### GitHub Push & README
- 解决 GitHub push 认证问题（HTTPS → SSH 切换建议）
- 撰写项目 README.md：含功能特性、技术栈、快速启动、项目结构、数据量限制、环境变量、路线图

### 高优先级 UI 改造（全部完成）
1. **Chat 页 Markdown 渲染** — 引入 react-markdown，替换掉 `replace(/\n/g, "<br/>")` 纯文本
2. **Insights 页** — 加分页（6条/页）、删除按钮（Popconfirm）、ECharts 图表渲染、Markdown 摘要、Key Findings 列表、内嵌表格
3. **Notebook 页** — 加删除 Cell（Popconfirm）、Markdown cell 编辑/预览模式切换（react-markdown 渲染）、Python cell Run 按钮、cell 类型标签徽章
4. **Data Tab Dataset 选择器** — 用 Select 替代只看 datasets[0]，支持多数据集切换 + 自动加载数据集列表
5. **ErrorBoundary + 404** — 新建 ErrorBoundary 组件（全局捕获异常，显示 500 页面）、NotFoundPage（404 页面带导航回首页），App.tsx 包裹路由

构建验证：TypeScript 通过，Vite 构建成功（3,058 KB JS + 247 KB CSS，1.26s）

### 端到端验证（Kaggle Superstore + DeepSeek）
- 从 Kaggle 下载 Superstore 数据集（9,994 rows × 21 columns）
- 配置 DeepSeek API（sk-352e2909...），更新 LLM 基址和模型名
- **修复 3 个 Bug**：
  - CSV 编码问题：Kaggle 文件是 Latin-1/Windows-1252 → 引入 chardet 自动检测
  - DuckDB register_csv 编码失败 → 改用 pandas 读取 + df.register 替代 read_csv_auto
  - 登录 API 字段不一致：token vs access_token
- **完整链路跑通**：注册 → 登录 → 创建项目 → 上传 CSV → Chat/SSE（7 个事件）→ DuckDB SQL 查询 → DeepSeek 分析 → Markdown 洞察
- AI 输出质量：结构化商业分析（Summary + Key Findings + Patterns + Recommendations）
- SSH key 配置完成，GitHub push 畅通
